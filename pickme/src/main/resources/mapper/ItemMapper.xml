<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="project.pickme.item.mapper.ItemMapper">

    <insert id="insertItem">
        insert into Item (customs_id, code, name, target, price, start_time, end_time, status)
        values (#{customsId}, #{code}, #{name}, #{target}, #{price}, #{startTime}, #{endTime}, #{status})
    </insert>

</mapper>
